rules:
  style:
    file-length:
      # repo-wide ceiling (applies to every .rego file, not just one); raised above
      # the 500-line default motivated by, but not limited to, the growing
      # source-review test suite, rather than fragmenting cohesive files.
      max-file-length: 800
